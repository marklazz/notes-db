(ns notes.storage)
;(defn classes []
  ;(let [db (d/db conn)
        ;classes
        ;(vec (map #(d/touch (d/entity db (first %)))
               ;(d/q '[:find ?class
                      ;:where
                      ;[?class :class/id]]
                 ;db)))]
    ;(generate-response classes)))
